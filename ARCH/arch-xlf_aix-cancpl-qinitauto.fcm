# AIX P7 at CCCma
# NCDF_INC    netcdf include file
# NCDF_LIB    netcdf library
# FC          Fortran compiler command
# FCFLAGS     Fortran compiler flags
# FFLAGS      Fortran 77 compiler flags
# LD          linker
# LDFLAGS     linker flags, e.g. -L<lib dir> if you have libraries in a
# FPPFLAGS    pre-processing flags
# AR          assembler
# ARFLAGS     assembler flags
# MK          make  
# USER_INC    additional include files for the compiler,  e.g. -I<include dir> 
# USER_LIB    additional libraries to pass to the linker, e.g. -l<library>


%NCDF_INC            -I/users/tor/acrn/rls/local/aix64/netcdf3.6.3/include
%NCDF_LIB            -L /users/tor/acrn/rls/local/aix64/netcdf3.6.3/lib -lnetcdf
%FC                  mpxlf90
%FCFLAGS             -qstrict -qfree=f90 -g -O3 -qrealsize=8 -qextname -qsource -q64 -qlargepage -qmaxmem=-1 -qfloat=nans -qinitauto=7FF7FFFF
%FFLAGS              -qstrict -qfixed -g -O3 -qrealsize=8 -qextname -qsource -q64 -qlargepage -qmaxmem=-1 -qfloat=nans -qinitauto=7FF7FFFF
%LD                  mpxlf90
# %LDFLAGS             -L/users/tor/acrn/rls/ords/coupler_archive -lcplcom
%FPPFLAGS            -P -C
%AR                  ar  
%ARFLAGS             rs
%MK                  gmake 
%USER_INC            %NCDF_INC
%USER_LIB            %NCDF_LIB
