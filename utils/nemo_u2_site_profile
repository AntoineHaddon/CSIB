#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~--------
# u2 specific environment consideration for NEMO
# Adapted from u2_site_profile in u2-v51-migration
# D. Yang, May 2022
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~--------
# TODO: move this stuff into the CONFIG/PLATFORM directory
export CCCMA_CONDA_PATH=/home/ords/crd/ccrn/scrd102/cccma_conda/4.12.0/bin
export PYCMOR_DEPS="/home/scrd102/canesm_libs/pycmor_deps"

# setup ORDENV environment
export ORDENV_SITE_PROFILE=20220421
export ORDENV_COMM_PROFILE=eccc/20220331
export ORDENV_GROUP_PROFILE=eccc/cmc/1.9.7
. /fs/ssm/main/env/ordenv-boot-20201118.sh > /dev/null 2>&1

# update environment to speed up short shell commands (from RPN-SI)
export EC_ATOMIC_PROFILE_VERSION=1.18.0
. $ARMNLIB_SHARE/check_profile_1.1.0
[ "$ORDENV_DIST" = "rhel" ] && export LD_PRELOAD=/lib64/libc.so.6:$LD_PRELOAD

# get default intel compiler, serial netcdf, hpcarchive, and maestro
# Notes:
#   - we should probably get the compiler information from the compilation env file?
#       - we should think of a way to avoid the intel hard coding
#. ssmuse-sh -x main/opt/intelcomp/inteloneapi-2022.1.2/intelcomp+mpi+mkl > /dev/null 2>&1
#. ssmuse-sh -x /fs/ssm/main/opt/hdf5-netcdf4/serial/static/inteloneapi-2022.1.2/01
. ssmuse-sh -x hpci/opt/hpcarchive-latest
. ssmuse-sh -d eccc/cmo/isst/maestro/1.8.4

# nco / cdo and who knows what else.
. ssmuse-sh -x eccc/cmd/cmds/ext/20220331

# setup proxies to make it so we can access external sites
export http_proxy=http://webproxy.science.gc.ca:8888/
export HTTP_PROXY=${http_proxy}
export https_proxy=${http_proxy}
export HTTPS_PROXY=${http_proxy}
